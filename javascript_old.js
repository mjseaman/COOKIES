// CookieBatch
function CookieBatch(options)) {
  var self = this

  initialize: function() {
    self.type = options.type;
  }
  initialize();
}

// CookieBatch.prototype

// CookieView
CookieView.prototype {
  placeBatch: function(batch) {
    $().append(BatchView(cookie))
  }
}

// CookieView.prototype

// Oven

// OvenView

// Baker
function Baker {

  function initialize() {
    $(document).on('click', '#make_batch', function(e) {self.newBatch});
    $(document).on('click', '#Back for 1 minute', function(e) {self.bakeOven});
  }
}

Baker.prototype {
  newBatch: function() {

  }

  bake: function() {
    //update oven function
    //render oven
  }

}




$(function() {
  var baker = new Baker();
});
